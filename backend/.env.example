# Base de datos
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=caso_listo

# JWT
JWT_SECRET=cambia-esto-en-produccion-por-algo-seguro

# Email (Gmail — usá una App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-app-password

# Frontend URL (para CORS y links en emails)
FRONTEND_URL=http://localhost:3000

# Puerto del servidor
PORT=3001
